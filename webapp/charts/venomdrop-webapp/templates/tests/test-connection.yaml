apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "venomdrop-webapp.fullname" . }}-test-connection"
  labels:
    {{- include "venomdrop-webapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "venomdrop-webapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
